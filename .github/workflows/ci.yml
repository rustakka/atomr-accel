# PR + push gate. Fmt → clippy → test → doc → Python bindings build.
#
# rakka-accel has a path dependency on a sibling rakka workspace; CI
# checks out both repos side-by-side before building.

name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

env:
  CARGO_TERM_COLOR: always
  RUSTFLAGS: "-D warnings"

jobs:
  # ─── Rust gate ────────────────────────────────────────────────────────
  fmt:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with: { path: rakka-accel }
      - uses: dtolnay/rust-toolchain@stable
        with: { components: rustfmt }
      - run: cargo fmt --all -- --check
        working-directory: rakka-accel

  clippy:
    runs-on: ubuntu-latest
    needs: fmt
    steps:
      - name: Checkout sibling rakka
        uses: actions/checkout@v4
        with:
          repository: rustakka/rakka
          path: rakka
      - name: Checkout rakka-accel
        uses: actions/checkout@v4
        with:
          path: rakka-accel
      - uses: dtolnay/rust-toolchain@stable
        with: { components: clippy }
      - uses: Swatinem/rust-cache@v2
        with: { workspaces: rakka-accel }
      - run: cargo clippy --workspace --all-targets --no-default-features -- -D warnings
        working-directory: rakka-accel
      - run: cargo clippy --workspace --all-targets --features rakka-accel-cuda/core-libs -- -D warnings
        working-directory: rakka-accel

  test:
    runs-on: ubuntu-latest
    needs: fmt
    strategy:
      fail-fast: false
      matrix:
        features:
          - "--no-default-features"
          - "--features rakka-accel-cuda/core-libs"
          - "--features rakka-accel-cuda/training-libs"
          - "--features rakka-accel-cuda/full-cuda"
          - "--features rakka-accel-cuda/replay"
          - "--features rakka-accel-cuda/cluster"
          - "--features rakka-accel-cuda/streams"
          - "--features rakka-accel-cuda/telemetry"
    steps:
      - name: Checkout sibling rakka
        uses: actions/checkout@v4
        with:
          repository: rustakka/rakka
          path: rakka
      - name: Checkout rakka-accel
        uses: actions/checkout@v4
        with:
          path: rakka-accel
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
        with: { workspaces: rakka-accel }
      - name: cargo check ${{ matrix.features }}
        run: cargo check --workspace ${{ matrix.features }}
        working-directory: rakka-accel
      - name: cargo test ${{ matrix.features }}
        run: cargo test --workspace ${{ matrix.features }}
        working-directory: rakka-accel

  docs:
    runs-on: ubuntu-latest
    needs: fmt
    # rustdoc warnings are uploaded as build logs but do not gate
    # the merge — the workspace has pre-existing broken-intra-doc-
    # link sites that are tracked separately. Promote to `-D
    # warnings` once those sites are cleaned up.
    steps:
      - name: Checkout sibling rakka
        uses: actions/checkout@v4
        with:
          repository: rustakka/rakka
          path: rakka
      - name: Checkout rakka-accel
        uses: actions/checkout@v4
        with:
          path: rakka-accel
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
        with: { workspaces: rakka-accel }
      - name: cargo doc --workspace --no-deps --all-features
        run: cargo doc --workspace --no-deps --features rakka-accel-cuda/full-cuda,rakka-accel-cuda/replay,rakka-accel-cuda/cluster,rakka-accel-cuda/streams,rakka-accel-cuda/telemetry
        working-directory: rakka-accel
      - uses: actions/upload-artifact@v4
        with:
          name: rustdoc
          path: rakka-accel/target/doc

  # ─── SemVer gate (PR only) ──────────────────────────────────────────
  semver-checks:
    name: semver-checks
    runs-on: ubuntu-latest
    needs: fmt
    if: github.event_name == 'pull_request'
    # Warn-only on 0.x pre-1.0 commits (per Cargo SemVer for 0.x);
    # promote to a hard fail by removing `continue-on-error` once any
    # crate hits 1.0.
    continue-on-error: true
    steps:
      - name: Checkout sibling rakka
        uses: actions/checkout@v4
        with:
          repository: rustakka/rakka
          path: rakka
      - name: Checkout rakka-accel
        uses: actions/checkout@v4
        with:
          path: rakka-accel
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
        with: { workspaces: rakka-accel }
      - name: Install cargo-semver-checks
        run: cargo install cargo-semver-checks --locked
      - name: Check public-API SemVer for every published crate
        working-directory: rakka-accel
        run: |
          set -e
          fails=0
          for crate in rakka-accel rakka-accel-patterns rakka-accel-train \
                       rakka-accel-agents rakka-accel-cuda-realtime; do
            echo "==> $crate"
            cargo semver-checks check-release --package "$crate" || fails=$((fails+1))
          done
          if [ "$fails" -gt 0 ]; then
            echo "$fails crate(s) failed semver-checks"
            exit 1
          fi

  # ─── Aggregate verify gate (mirrors xtask verify) ───────────────────
  verify:
    name: verify (release-pipeline gate)
    runs-on: ubuntu-latest
    needs: [fmt, clippy, test]
    steps:
      - name: Checkout sibling rakka
        uses: actions/checkout@v4
        with:
          repository: rustakka/rakka
          path: rakka
      - name: Checkout rakka-accel
        uses: actions/checkout@v4
        with:
          path: rakka-accel
      - uses: dtolnay/rust-toolchain@stable
        with: { components: clippy, rustfmt }
      - uses: Swatinem/rust-cache@v2
        with: { workspaces: rakka-accel }
      - name: cargo xtask verify
        run: cargo xtask verify
        working-directory: rakka-accel

  # ─── Python bridge gate ──────────────────────────────────────────────
  python:
    runs-on: ubuntu-latest
    needs: fmt
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.11", "3.12"]
    steps:
      - name: Checkout sibling rakka
        uses: actions/checkout@v4
        with:
          repository: rustakka/rakka
          path: rakka
      - name: Checkout rakka-accel
        uses: actions/checkout@v4
        with:
          path: rakka-accel
      - uses: dtolnay/rust-toolchain@stable
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - uses: Swatinem/rust-cache@v2
        with: { workspaces: rakka-accel }
      - name: Install build tools
        run: pip install maturin pytest numpy
      - name: maturin develop
        run: maturin develop --release --no-default-features --features extension-module
        working-directory: rakka-accel/crates/rakka-accel-py
      - name: pytest
        run: pytest tests/ -v
        working-directory: rakka-accel/crates/rakka-accel-py

  # ─── Aggregate gate (required for branch protection) ─────────────────
  ci-pass:
    runs-on: ubuntu-latest
    needs: [fmt, clippy, test, docs, verify, python]
    if: always()
    steps:
      - name: All required jobs passed
        if: ${{ !contains(needs.*.result, 'failure') && !contains(needs.*.result, 'cancelled') }}
        run: echo "ok"
      - name: A required job failed
        if: ${{ contains(needs.*.result, 'failure') || contains(needs.*.result, 'cancelled') }}
        run: exit 1
